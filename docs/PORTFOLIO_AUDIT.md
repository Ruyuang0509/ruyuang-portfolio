# 作品集稽核紀錄

## 2026-07-28 全域程式碼品質稽核

- 稽核先由 clean `main`／`3cab18c` 建立 production build 基線，再逐項檢查 component、copy、naming、CSS、effect、Web Audio、motion、media、dependency、Pages 與 artifact；完整證據見 [`CODE_QUALITY_AUDIT.md`](CODE_QUALITY_AUDIT.md)。
- Site identity／11 段 IA、Power BI 九章內容、testing metric key 與共用圖片 renderer 已收斂；不可達 component／data／selector／export與21個 media孤兒移除。
- 新增 `audit:quality`／`audit:site`，`audit:media` 改為 exact path＋missing file gate。`doctor`／publication均exit 0；sound 18/18、rights 15/15、scanner 73/73。
- Fresh submission 為473 modules、entry 100968 B、CSS 45757 B、initial JS gzip 183291 B、119 files／32 text files；98個public files全數0 missing／0 hash mismatch。
- Browser 1440／768／390／320 為global overflow、dead anchors、duplicate IDs、broken images、console issue全0；menu、lazy deep link、storyboard keyboard／seek與Web Audio start／stop通過。
- 未 commit／push／deploy；人工 accessibility、production field與第三方 rights限制保持未驗證。

## 2026-07-28 Power BI 案例九章 IA 與 responsive 驗證

- `learning-dashboard-analysis` 改以 lazy `LearningDashboardProjectDetail.jsx` 與 case-scoped CSS 呈現九章；其他案例仍使用共用 renderer，既有全站色彩、繁中排版、圓角、surface、focus 與 motion 規範未重設。
- 1240px shell 與 12 欄配置只作用於此案例；低於 1024px 轉單欄，九章導覽依 viewport 為 3／3／1 欄。公開案例包含 3 個分析問題、7 個流程步驟、3 組圖表判讀、4 項互動功能、3 張倫理／限制卡與 4 項後續改善。
- 案例維持 text-only：受限 Power BI／Excel、實際資料、真實截圖、操作媒體與分析結果仍隔離；公開頁不把抽象 SVG、重製圖表、假介面或影片外觀冒充成果，公開與隱私邊界集中於第 08 節。
- Submission 以 1440／1280／1024／768／390px 核對 document／article overflow、out-of-bounds 與 text overflow 均為 0，console error／warning 為 0；案例與 charts 的冷啟動 hash 均定位成功。
- 該次完整 `pnpm run doctor` exit 0；內容檢查仍保留「流程圖與架構圖、媒體證據」建議群組缺件警告，表示受限真實證據未以假圖補位。這不擴張 Hamlet limited-use rights、研究／學習成效、production 或人工可及性驗證範圍。

## 2026-07-26 Hamlet Rights Phase B 與公開展示版整合

- 蕭智仁以 2026-07-26 具名確認四項 applicant attestation，並綁定交付影片 SHA-256 `7E50B6EB01E646FB822D6384D73C9C01A08DC9DF8D5D22A3102B777A460D2312`。
- Manifest 為 `rightsReview.status: verified`、`applicantAttestation.confirmed: true`、`publicationGate: approved`；Suno commercial use、ads、paywall、affiliate revenue、商業廣告與音樂發行仍維持 false／不允許。
- Phase B base 的完整 `pnpm run doctor`、14/14 rights tests、58/58 scanner fixtures、18/18 sound tests、draft／submission builds、Pages audit 與 `pnpm run check:publication` 均 exit 0。Fresh submission `dist/` 為 132 files／25 text files，118 個 `public/` files 0 missing／0 hash mismatch。
- Phase B base 的 In-app Browser 在 1280×720、768×1024、375×812、320×568 核對 confirmed disclosure、Suno focus、40 秒影片 keyboard play／pause、8 幕、2 tracks 與 responsive width；待本人確認、舊權利卡片、duplicate ID、broken case target、global horizontal overflow 與 clean-tab console error 均為 0。
- 原始八幕生成紀錄、原始 EML 與可編輯 Canva 專案仍未找到；這些限制、`usedForExistingVideo: false` 與 `testing.statusKey: notValidated` 均保留，沒有把 applicant attestation 升格成獨立原始證據或研究成效。
- 公開展示版把 public admission data 與 Draft-only `admission-evidence.audit.js` 分層，並以動態拆分守住 entry budget；另新增深色語意閱讀面、集中 theme endpoint、lazy deep-link 重算、print reading-surface reset，並修正 REAPER 過度敘述。
- 本輪 `pnpm install --frozen-lockfile`、完整 `doctor` 與 `check:publication` 均 exit 0；18/18 sound、14/14 rights、73/73 scanner fixtures 通過。Draft 為 471 modules／200889 B initial JS gzip／180733 B entry／44315 B CSS；submission 為 467／192936 B／153704 B／44315 B。
- Scan 為 132 files／25 text files、67／9 rules；118 個 `public/` files 在 132-file `dist/` 中為 0 missing、0 hash mismatch。Manifest 維持 `verified / approved`。
- Browser 在 1280×800、768×900、390×844、320×720 為 0 overflow、0 broken hashes、0 duplicate IDs、0 broken images、console 0 warning／error；四個 deep links 位於頂端約 95–112 px，theme endpoints 與 mobile Escape 還焦通過。

## 2026-07-27 作者視角、作品索引與案例視覺整合

- 公開文案依「作者親自做了什麼／可核對 artifact 或方法／仍不能推論什麼」三層重整；這是語氣與閱讀層級整合，不改寫既有學歷、角色、權利、測試或研究結論。
- 11 個高階 IA 不變；`#project-index` 是 `#selected-work` 內的巢狀快速索引，不得計成第 12 個高階段落。索引順序固定為 Web Audio → Hamlet → Power BI → 資料視覺化，且只提供真實案例 anchor。
- Web Audio 的 N1／N2／N3 圖改為正式視覺策略卡片；資料視覺化案例改由 `productionWorkflow` 與 lazy `CaseProcessSection` 輸出四階段語意 HTML／CSS，不再依賴內含文字且會被裁切的舊流程 SVG。
- Hamlet 仍為 `rightsReview.status: verified`、`publicationGate: approved` 的限定非營利用途；private originals 未找到、commercial use 不允許與 `notValidated` 不因此次內容整合而改變。
- 下方 2026-07-26 命令、bundle、inventory 與 Browser 數字仍是整合前基線。2026-07-27 最新組合已完成完整 `doctor`、獨立 publication gate、dist 定向文字檢查前置 build 與 targeted Browser smoke；精確結果見 PA-051。未執行的 production／人工項目不得預先寫成通過。

## 2026-07-26 Hamlet rights Phase A（歷史快照）

- Schema v2、public redacted summary、attestation 草稿、純函式 rights validator、13 個 Node fixtures、公開 Suno credit 與 Pages publication gate 已建立。
- Phase A install、workspace、media／text／CJK／evidence audits、content check、13/13 rights tests、58/58 scanner fixtures、18/18 sound tests、draft build 與 `check:submission` 均 exit 0；`check:publication` exit 1，列出 25 個 status／evidence lifecycle／attestation／具名 required-check blockers，符合本人簽核前的 fail-closed 設計。
- 私人證據盤點沒有找到原始 EML 或八幕生成對話；current tracked、untracked 與 19 個 commits 歷史沒有 EML、私人 evidence path 或完整郵件標頭洩漏。
- 公開 copy 已把「原始 instrumental／no lyrics 規格」與「實際含英語歌詞與人聲」分開。Rights verification 不等於形成性測試、研究或學習成效驗證。
- In-app Browser 四個 viewport 實測：Suno credit／pending attestation 可見，link focus-visible，影片鍵盤播放／暫停有效，8 張 storyboard、2 tracks、11 anchors 保留，duplicate ID／global overflow／console warning or error 均為 0。

## 2026-07-26 公開展示版稽核結論

- 公開 Admission 資料已與 `admission-evidence.audit.js` 分離；public schema 只保留自然作品敘事，Draft／Audit 以相同 stable ID 保留 evidence、validation、rights、limitations 與 requests。Draft-only audit data 改為動態拆分以修正 entry budget；validator 會核對兩層一對一關係，submission scanner 則只對實際公開輸出 fail closed。
- 原資料視覺化區段曾讓透明 section 上的亮色文字穿過 mist／paper 中間色。修正後 `DataVisualizationSeries` 使用固定、不透明的深色語意閱讀面，field 動畫只出現在閱讀面周圍；theme endpoint 集中同步 navigation、ScrollTrigger update／refresh 與離散 endpoint。`App` 的 `ResizeObserver` 會在 lazy 內容造成高度變化時重算 deep link。
- Print 已重設 reading surface，REAPER copy 已降回實際學習狀態。本輪完整 `doctor`、73/73 scanner、bundle／inventory、四 viewport Browser smoke 與 publication gate 均通過；精確數字以上方 current section 與 PA-044 為準。
- Hamlet Phase B 的 applicant attestation、limited-use `verified / approved` 與 `check:publication` exit 0 不因整合而倒退；commercial use 仍不允許，private originals 仍未找到，研究與學習成效維持 `notValidated`。

## 目前 source、部署與驗證

- 目前高階錨點為 `#top`、`#sound-transition`、`#reviewer-path`、`#interactive-sound-learning`、`#pure-data-learning`、`#research-positioning`、`#selected-work`、`#collaboration`、`#learning-roadmap`、`#ai-workflow`、`#contact`；`#research-proposal` 只保留為研究構想的相容 alias。
- `#project-index` 巢狀位於 `#selected-work`；中寬以上 2 欄、窄版 1 欄，依 Web Audio、Hamlet、Power BI、資料視覺化排序。它不改變 11 段高階 IA，也不建立假篩選、空連結或未存在的 GitHub／Live Demo。
- `#selected-work` 的內部順序固定為《畫本》→ 其他可驗證作品（AI 文學故事 MV、資料視覺化、Power BI）→《希望有羽毛和翅膀》個人 MV 混剪。兩件文字型新案例已存在；舊「不新增案例」決策已被較新的申請者事實與選件決策取代，但公開成片與逐項 rights／credit 仍未提供。
- Pure Data v0.2.1 MP4 與 poster 實際存在於 `public/media/portfolio/pd-crossmodal-mapping-v0.2.1-operation-demo.mp4` 與 `public/media/portfolio/pd-crossmodal-mapping-v0.2.1-operation-demo-poster.png`。這只證明本機功能操作有錄影，不證明申請者獨立完成 Patch、完成使用者／學術驗證或建置正式研究系統。
- 原始 Pure Data 畫面含 `validated` 與本機 D 槽路徑；公開 copy 已把前者限制為「本機功能測試」，但影片畫面仍會暴露路徑。這是本輪 submission/publication 前必須人工接受或重錄的風險。
- 私人研究工作區內確有研究計畫 DOCX，但尚未核准公開，也沒有公開下載。文獻、設備、樣本、預算、倫理、metadata 與公開範圍仍需核對；不得把它寫成「沒有檔案」，也不得因檔案存在而推論研究已通過審查。
- Metadata source 已改為「蕭智仁｜聲響、互動與數位學習作品集」，canonical／OG URL 為正式 GitHub Pages，`llms.txt` 列出 11 個錨點。2026-07-27 最新組合的 fresh submission build、scanner、targeted anchors 與 publication rights audit 已通過；manifest 為 `verified / approved`。目前沒有建立、推送或部署新的 PR，production deployment／HTTP、screen reader、真實 zoom、system reduced-motion、實機、多瀏覽器音訊，以及兩個 YouTube 作品的第三方 rights／完整 credit 尚未核對。

更新日期：2026-07-28。這份文件記錄可由原始碼、建置輸出與本機 rendered behavior 證明的結果；內容或學術決策不足時，不以推測代替申請者證據。

## 判讀方式

- 嚴重度使用 `Critical`、`High`、`Medium`、`Low`。
- 「是否確認」區分為程式碼／rendered behavior 已確認、依賴使用者素材、依賴學術／內容決策，或僅能部分確認。
- 「已完成」只表示本輪工程修正與本機驗證完成，不代表正式使用者研究、輔具測試、GitHub 遠端執行或學校審查已完成。

## 稽核矩陣

PA-001–PA-037 保留先前驗證 lineage；PA-038–PA-046 記錄 2026-07-26 公開展示版基線，PA-047–PA-051 記錄 2026-07-27 整合，PA-052 記錄 2026-07-28 Power BI 案例重構。舊 row 的「已完成」與 Browser 數字只適用當時 fingerprint；新組合驗證完成後再更新，不得沿用舊數字。

| ID | 類別 | 頁面／元件 | 證據 | 對評審或使用者的影響 | 嚴重度 | 是否確認 | 修正方式 | 本輪狀態 | 驗證 |
|---|---|---|---|---|---|---|---|---|---|
| PA-001 | ADMISSION_NARRATIVE | Hero、Research Positioning、案例順序 | `portfolio.js` 與 `App.jsx` 將聲響研究命題、旗艦 Web Audio、學習歷程置於支持案例之前 | 評審可先理解申請方向，再判讀證據強弱 | High | 程式碼與 rendered behavior 已確認 | 保留「聲音 × 互動 × 數位學習」主線，AI 方法置於旗艦證據之後 | 已完成 | 2026-07-24 submission preview 於 1440×900／375×812 確認 current section order、Hero 主張與 CTA；兩者 0 overflow／broken image |
| PA-002 | CONTENT | 旗艦 Web Audio 案例 | 公開狀態仍為 `notValidated`，只有 planned methods | 不應把可操作原型誤讀為已證明學習成效 | High | 依賴使用者素材 | 執行形成性任務並補匿名觀察、錯誤、口述理解與限制 | 待申請者補證據 | `content:check` 保留未驗證狀態 |
| PA-003 | CONTENT | Learning Trail | Pure Data／REAPER 明列「學習中」，沒有 patch、session 或聲音輸出 | 敘事誠實，但聲響製作證據仍偏薄 | High | 依賴使用者素材 | 先選一個最小可公開 artifact，附 signal flow、輸出與反思 | 未完成 | rendered copy 與 submission scan 已確認未誇大 |
| PA-004 | FUNCTION | Navbar 桌面／行動 anchor | 原本只有行動連結把焦點移入內容；桌面鍵盤 Enter 會留在 fixed nav | 鍵盤使用者可能重複巡覽導覽列，無法直接從剛選擇的內容續讀 | High | rendered behavior 已確認 | 桌面鍵盤 Enter 與行動選單聚焦目標 heading；lazy 區段聚焦永久 section wrapper；桌面滑鼠保留 link focus；Escape 還焦 trigger | 已完成 | 1440×900 桌面 Enter 與 375×812 行動點擊 `#research-proposal` 均落在 96 px、焦點進入永久 wrapper 且 `aria-current` 正確；行動 Escape 還焦 trigger |
| PA-005 | VISUAL | 暖紙主題文字與 accent | 舊 accent 在淺色表面對比不足；研究卡內的 meta／muted copy 也曾錯誤繼承深色區亮色 | 連結、標籤與說明文字在暖紙表面可能難以辨識 | High | 程式碼、Lighthouse 與 rendered behavior 已確認 | 調深 light-theme accent；paper panel 的 contextual copy 明確使用 inverse ink text | 已完成 | `#405016` 對 `#d8cfbd` 5.71:1；Lighthouse color-contrast 通過；browser computed style／screenshot |
| PA-006 | RESPONSIVE | Research Positioning、Learning Trail display headings | 320px 時兩個片語單位曾超出 viewport；頁面整體沒有水平捲動 | 最窄手機可能出現局部文字被切邊 | High | rendered behavior 已確認 | 重新拆分繁中 phrase units，不縮小整體字級 | 已完成 | 320×568 retest：0 個越界 heading、0 global overflow |
| PA-007 | COPY_UX | 聲音狀態與送審閱讀出口 | 聲音狀態可讀；頁尾只提供「閱讀研究構想／操作 Web Audio 原型／回到代表作品」三個真實站內 CTA；repository 仍沒有公開聯絡資料或外部個人連結；live status 曾位於 busy ancestor 內 | 不用假 CTA、含糊錯誤或被延後的啟用中回饋誤導使用者 | Medium | 程式碼與 rendered behavior 已確認 | 新增「聲音啟用中」與可理解失敗文案；`role="status"` 移出 busy 控制群組；維持真實站內 CTA | 已完成 | 啟用後顯示「聲音播放中」；Escape 後為「聲音已停止」；頁尾三個 target 存在且 reviewer 區不殘留錯誤 `aria-current` |
| PA-008 | ACCESSIBILITY | SoundInteractionPrototype | 原型原有三個鍵盤 range；速度／濾波亮度只能靠 pointer movement 表達；pointer pad 的 `aria-label` 曾放在無有效 role 的 `div` | 只用鍵盤者無法操作全部四個 mapping，輔具也可能忽略 pad 說明 | High | 程式碼、Lighthouse 與 rendered behavior 已確認 | 新增「濾波亮度」range、readout、描述關聯與節流 live announcement；pad 以具完整說明的 `role="img"` 呈現，鍵盤操作仍由 ranges 提供 | 已完成 | 4 個 range；pad accessible name 正確；Lighthouse `aria-prohibited-attr` 通過 |
| PA-009 | ACCESSIBILITY | 全站 | 真實 screen reader、200% browser zoom、reduced-motion 模擬與實機觸控尚未完成 | source-level 規則不能替代輔具與實機驗收 | High | 部分確認 | 由申請者或測試者以 NVDA／VoiceOver、真實 zoom、真機補矩陣 | 未完成 | 本輪只完成鍵盤、focus、contrast、viewport 與 source-level reduced-motion 檢查 |
| PA-010 | AUDIO_MEDIA | Web Audio controller 與聲音原型 | AudioContext `resume()` 可能 pending，stop fade 曾在斷線後失效，context interruption 也可能留下假 `running` | 控制項可能卡住、停止產生突兀切斷，或使用者離開後才延遲啟動 | High | 程式碼、Node tests 與 rendered behavior 已確認 | 抽出可測 controller；加入 cancellable resume、graceful release、hidden immediate cleanup、statechange、建圖失敗與 destroy cleanup；React 停止／Escape／離屏也取消 pending start | 已完成 | 13 個 lifecycle tests；rendered starting 期間停止按鈕可用並回到「聲音已停止」；0 runtime console error |
| PA-011 | AUDIO_MEDIA | 影像、影片、Power BI | 公開媒體為本機 AVIF/WebP/MP4 與系列／索引用安全 SVG；Power BI 專屬案例內頁為 text-only，實際資料、截圖、操作媒體與結果隔離；YouTube 字幕品質未人工確認；hidden-only asset gap 另見 PA-020 | 避免敏感素材外洩與假圖誤導，但影片快速審查與聽覺可及性仍有缺口 | High | 資料使用說明、使用者素材與公開邊界已核對 | 維持 Power BI 隔離；抽象 SVG 不作成果證據；人工核對另一資料案例的字幕與 transcript；只有另取得資料提供方明確許可後才重新評估真實結果展示 | 進行中 | `audit:media` 對 remote／restricted 規則通過；本輪 Power BI submission case 未載入截圖、操作影片、分析檔或假播放器 |
| PA-012 | PERFORMANCE | Hero R3F chunk 與 Hero copy | 歷史單一 Three chunk 約 851 kB minified／224 kB transfer；全頁／主標隱藏式進場曾使 LCP 落在 nav，Three 也曾過早與首屏競爭。2026-07-23 fresh build 的 lazy 3D closure 為 638680 raw／169383 gzip B，最大 chunk 483687 B | 已消除 oversized warning 與 initial preload；低階裝置的 GPU／耗電風險仍需真機證據 | Medium | fresh build 與歷史 immutable Lighthouse archive 已確認；現行公開文案 fingerprint 尚未重跑 Lighthouse | 主標／介紹首幀可見；lean R3F canvas 只 extend 必要 constructors；Three 以 visibility-aware lazy gate 載入，局部 scene boundary 保留核心文案；遞迴 built-import budget 防止 closure 或單一 chunk 回歸 | Build closure 已完成；現行 Lighthouse、真機／field evidence 待辦 | PR #5 前歷史 archive：mobile Performance 94／Accessibility 100／LCP 2651 ms／TBT 90 ms；desktop 100／100／560 ms／0 ms。PR #5 已改動 LCP 文字，這些不是 current-fingerprint 數據 |
| PA-013 | RELIABILITY | React root | 原本只有 section boundaries，Navbar 等根層錯誤可能讓整站空白 | 單一未捕捉錯誤可能中斷審查 | High | 程式碼已確認 | 在 `main.jsx` 外層加入可重新載入的 `RootErrorBoundary` | 已完成 | submission build 成功；fallback source 已檢查 |
| PA-014 | GITHUB_PAGES | Vite、public assets、`dist/` | 原本 `base: /` 與 `/media/...` 會破壞 project-site 子路徑 | 部署到 `user.github.io/repository/` 時資產 404 | Critical | 建置輸出已確認 | 預設相對 base、以 `BASE_URL` 組 public assets、加入 `audit:pages` | 已完成（本機） | `check:submission` 的 Pages audit 通過 |
| PA-015 | GITHUB_PAGES | Actions workflow | 原本沒有部署流程；Windows-only `.ps1` scripts 不適用 Ubuntu build job。現有 workflow 在 push 到 `main` 或手動觸發後，以 Windows 驗證、Ubuntu deploy | Pages 建置可重現，submission 與 publication gates 均在 artifact configure／upload 前執行；PR-stage CI 仍未建立 | High | 設定檔、本機 build、舊 GitHub Actions／deployment 與 production HTTP 已確認 | Node 22／pnpm 11.7、`windows-latest` build、submission → publication → Pages artifact deploy；另評估 PR-stage CI | Production gate 已完成；PR gate 待辦 | 2026-07-26 workspace check 驗證 gate 順序且拒絕 `continue-on-error`；舊 `695b520` run `29680534295` success 只代表較早 fingerprint |
| PA-016 | RELIABILITY | Repository metadata | 先前工作區曾只有空 `.git`；目前 Git history、remote、branch 與 PR lineage 都可確認 | 錯認 repository 狀態會造成重複初始化、錯誤 push 或沿用已合併 PR branch | Critical | 本機 Git 與 GitHub PR API 已確認 | 每輪重新查 refs／PR；下一輪實作由更新後的 `main` 建立新 `codex/` branch | 已完成 | `main`／`origin/main`=`695b520`；working local／remote=`61ea9d8`；兩者 tree identical，PR #1–#5 皆 merged |
| PA-017 | AI_DISCLOSURE | `#ai-workflow` 與 `docs/ai-workflow/` | 原本沒有公開、可追溯的人機責任與 prompt 演進入口 | 評審無法分辨 AI 協助、作者決策與不可代填邊界 | Medium | 程式碼、文件與 rendered behavior 已確認 | 新增低比重方法區段、Prompt v1／v2／v3 摘要、三個完整真實失敗鏈；repository 保留 v1／v2 完整文字，v3 完整任務仍只在外部附件 | 已完成 | 1440×900 rendered section；submission build 含三組責任、三個提示詞版本與三個失敗鏈 |
| PA-018 | TESTING | scripts、sound controller 與 browser matrix | 原本只有 5 個 mapping tests，AudioContext race／cleanup 及桌面 nav focus 無可重跑證據 | 回歸可能直到瀏覽器操作或送審才被看見 | High | 本機已確認 | `test:sound` 擴充至 18 tests；current browser smoke 覆蓋桌面／行動研究構想導覽、行動 menu 與聲音啟停 | 已完成（本機） | 18/18 tests；1440×900 Enter 與 375×812 menu／Escape focus assertions；Web Audio 啟用與 Escape 停止；`doctor` 通過。較大舊矩陣只作歷史證據 |
| PA-019 | ACCESSIBILITY | 全站長頁 scrollbar 與 320 px reflow | 全域 CSS 曾隱藏 Firefox、WebKit 與舊 Edge scrollbar；恢復 15 px 平台 scrollbar 後，硬編碼 `body min-width: 320px` 會在 320 px viewport 產生 15 px 水平溢位 | 評審缺少長頁位置與可捲動 affordance；最窄 viewport 可能橫向捲動 | Medium | 程式碼與 rendered behavior 已確認 | 恢復平台 scrollbar，使用 theme token 配色；`body` 最小寬度不得超過可用寬度 | 已完成 | 1440×900 scrollbar computed `auto` 且深／暖紙兩端配色正確；1440×900、375×812、320×568 均 0 global horizontal overflow，滑輪捲動有效，console error／warning 0 |
| PA-020 | SUBMISSION_BOUNDARY | Hidden immersive media | 13 個 `ph-after-*`／`mv-soft-*` placeholder 只屬 hidden case，公開案例零共用；歷史 copies 留在 ignored reports | 未公開視覺／影片不可由正式 URL 或 dev root 直接取得 | Critical | source、fresh dist 與 submission dev HTTP 已確認 | 移除 13 個 public assets／generator items／失效 captions；hidden case 改用空 media state；submission dev deny reports／restricted／internal 路徑 | 已完成 | 舊 canonical URL 13/13 為 404；restricted／internal／historical copy 為 403；fresh dist inventory 0 match |
| PA-021 | SUBMISSION_BOUNDARY | Built copy 與 scanner | 舊 scanner 只掃 31 個文字 terms，曾在已知 leaks 存在時 exit 0；中文「佔位／尚未提供」、內部選件規則與私人 evidence inventory 亦曾漏網 | 自動門檻假陰性會讓內部語彙或 binaries 進入 formal output | Critical | core、CLI、fixtures 與 fresh submission artifact 已確認 | 拆分 injectable core／thin CLI；保留 public-copy 高訊號規則、rights／private-evidence inventory 與有界否定語境，避免把誠實限制誤判為成果主張 | 已完成（本機） | 73/73 fixtures、67 個 text rules／9 個 inventory rules；scan 132 files／25 text files，`check:submission` 與完整 dist inventory 通過 |
| PA-022 | PUBLIC_METADATA | `llms.txt`、favicon、social preview、JSON-LD、案例 SEO | 舊品牌與 dead anchors 曾原樣進 dist | 品牌、machine-readable summary 與實際資訊架構必須一致 | High | public files、source anchors、fresh dist 與 scanner 已確認；Draft PR 尚未 deploy | 統一為 `蕭智仁｜聲響、互動與數位學習作品集`；description 明列 Web Audio、Pure Data、影音作品與混合監聽研究構想；canonical／OG URL 使用已確認的 Pages project URL，`llms.txt` 只列有效 11 anchors | 本機驗證完成；production 待 merge 後重驗 | 118 個 public files 在 132-file dist 為 0 missing／mismatch；目前 metadata、social preview、`llms.txt` 與 submission build 已比對，production HTTP／分享卡尚未重驗 |
| PA-023 | PERFORMANCE_EVIDENCE | Lighthouse lineage | 既有 build inputs 已建立 immutable submission artifact、source manifest 與 mobile／desktop raw reports；PR #5 後 source hashes 與 archive manifest 不同 | 歷史分數只解釋舊決策，不能代表新版公開文案或 production field | Medium | archive manifest、目前 source hashes 與 LCP node 舊文案已比對 | 對現行 source 重跑 Lighthouse；每次 runtime、公開 LCP 文字、build 或 audit script 變更後都重新建立 fingerprint | 待重跑現行 fingerprint | 歷史 run `2026-07-17T17-31-33-225Z`：mobile 94／100、LCP 2651 ms、TBT 90 ms；desktop 100／100、LCP 560 ms、TBT 0；兩者 CLS 0 |
| PA-024 | RESPONSIVE | Hero 首屏 | 主標在 1440×900 曾把研究介紹與 CTA 推到第一屏之外；長句在窄螢幕也缺少一致的片語換行 | 評審進站時無法立即看到主張後的研究說明與下一步 | High | 本輪 1280×800、768×900、390×844、320×720 rendered behavior 已確認；1440×900／375×812 保留為先前 fingerprint 證據 | 主標改用共用 `EditorialHeading`，保留完整可及名稱並收斂 mobile／desktop fluid type | 本輪四個 viewport 回歸通過 | 四組 viewport 均為 0 global overflow、0 broken image、0 console warning／error；真實裝置與未列尺寸不在本輪覆蓋範圍 |
| PA-025 | CONTENT | 兩件資料視覺化案例標題 | `titleLines` 曾省略「應用探討」與「分析」，與完整 `title` 不一致 | 索引與案例頁的視覺標題會截斷作品名稱 | High | 程式碼、validator 與 rendered behavior 已確認 | 補齊完整片語分行；validator 將 `titleLines` 攤平後與 `title` 比對 | 已完成 | `content:check` 通過 5 件；320／375 px 無片語越界 |
| PA-026 | FUNCTION | 作品索引與深層 case hash | `content-visibility:auto` 的 placeholder 會讓後段案例 fragment 先落在舊 scroll range，直接載入或點擊最後案例可能停在錯誤位置 | 評審點案例後看不到所選作品開頭 | High | rendered behavior 已確認 | 目標案例暫時維持完整 layout，重算 Lenis 後以有界 settle 重試定位 | 已完成 | 直接載入與實際點擊 `#learning-dashboard-analysis` 均落在 fixed nav 下約 96 px，hash 與 active nav 正確 |
| PA-027 | ACCESSIBILITY | Navbar 與主要站內 CTA | 導覽沒有目前位置語意與 active state；部分 compact chip 的實際高度低於 44 px | 長頁中不易判斷所在區段，觸控命中區偏小 | Medium | rendered behavior 已確認 | 依 fragment／可見區段設定 `aria-current="location"`；Navbar、Hero CTA、研究跳轉與 reading map 目標至少 44 px | 已完成 | 375×812 visible target audit 0 個小於 44 px；行動 menu Escape 關閉並還焦 |
| PA-028 | FUNCTION | 旗艦案例閱讀導覽 | `#interactive-sound-learning-media` 沒有對應 section，但閱讀導覽曾產生連結 | 站內導覽會跳向不存在的目標 | High | Current submission DOM 已確認 | 共用 renderer 只在 supporting media 實際可渲染時加入媒體錨點 | 已完成 | 2026-07-24 的 1440×900 current regression 盤點 88 個站內 hash links、0 broken target、0 duplicate ID；375×812 亦為 0 broken target／duplicate ID |
| PA-029 | VISUAL_MOTION | 深色支持證據 → 暖紙作品區 | 前一輪以 section 上的 96–240 px linear-gradient bridge 模擬轉場，只會穿過固定色帶，不能停在全畫面中間色或反向 scrub | 與初代敘事 motion 不一致，也以額外 layout 高度製造過場 | High | 程式碼與 rendered behavior 已確認 | 改為 fixed full-viewport、pointer-inert field；由實際 section geometry 連續控制 paper／mist／3 radial layers，只動畫 opacity／transform，不插值 root／foreground tokens | 已完成 | 1440×900 range 823 px／0.914 vh，375×812 range 712 px／0.877 vh；中段全畫面暖灰、停止保持、反向可逆、0 overflow、console 0 issue |
| PA-030 | FUNCTION_ACCESSIBILITY | Prompt Template、7 個圖解長描述、雙語逐字稿與行動選單 | Native disclosure 與 mobile nav 開／關曾瞬間改變高度，長逐字稿造成明顯 layout jump | 開關狀態缺少連續 feedback，鍵盤與長內容閱讀節奏突兀 | High | 程式碼與 rendered behavior 已確認 | `AnimatedDetails` 以實際 height 動畫兩向開／關並保留 native semantics；indicator／content／`aria-expanded` 同步，完成後刷新 Lenis／ScrollTrigger。Mobile menu 沿用 Motion；reduced motion 立即切換 | 已完成 | 滑鼠、快速反轉、Enter、Space、Escape／focus restore 通過；375 px transcript 約 70→2056→70 px，0 horizontal overflow；print 展開內容 |
| PA-031 | PUBLICATION_GOVERNANCE | Hamlet media／Pages deploy | Manifest schema v2、具名 applicant attestation 與 pre-upload publication gate 已建立；Suno 限定非營利條件維持 fail closed | Current branch 的 repository gate 與申請者權利聲明已對齊；用途變更或 private-original 誤述仍可能超出核准 | Critical | manifest、workflow、tests、applicant message 與 Browser disclosure 已確認；原始生成紀錄／EML／Canva project 未找到 | 維持限定用途、公開刪節摘要與 private-original 分層；任何營利用途先重新授權 | 已完成（limited-use） | 2026-07-26 `check:publication` exit 0、manifest `verified / approved`；workflow 在 configure／upload 前執行 gate。本輪不 merge 或 deploy |
| PA-032 | CONTENT_GOVERNANCE | 第一人稱公開文案與證據邊界 | PR #5 將抽象／英中混用說明改為自然第一人稱繁中，並在案例中分開實際成果、未驗證結果、不能推論與下一步；metadata／ARIA／UI labels 同步 | 降低模板語氣與誤讀研究成果的風險；保留現有 IA、互動與動效敘事 | Medium | `ca956c9..61ea9d8` source delta、public data、components、metadata 與 production bundle 已確認 | 維持「作者行動 → artifact／方法 → 限制」句型；Web Audio／Power BI 視覺圖只稱視覺方向或版面概念，不冒充流程／架構圖 | 已完成 | 公開案例仍 4 件、hash target 集合不變；AI roles 4→3、資料案例 extended sections 5→2、Power BI 6→3；runtime／styles／motion／dependencies 無 delta |
| PA-033 | ADMISSION_NARRATIVE | Hero、實體 IA、研究構想 | 原本泛稱聲響／學習研究，Web Audio 與長篇研究軌道之間缺少清楚的背景轉向與申請階段研究構想分界 | 評審可能把可操作原型、工具學習與尚未建置的混合監聽研究混為一談 | High | 此 row 為先前 Admission Evidence Pass；當時的 source／rendered behavior 只適用舊 fingerprint，官方簡章未獨立核對 | 收斂為背景轉向 → Web Audio → 學習邊界 → 代表作品 → 研究構想；新增 standalone proposal 與可調整方法聲明 | 歷史實作已完成；本輪 Browser smoke 通過 | 當時的 1440×900／375×812 結果仍只屬舊 fingerprint；目前整合的自動與 Browser 驗證見 PA-044、PA-045 |
| PA-034 | AUDIO_EVIDENCE | Web Audio、Pure Data、REAPER | Web Audio 公開流程缺少正規化／映射兩段；Pure Data／REAPER 的工具狀態不足以證明作者能力 | 聲音主線可能被誤讀為標籤或 AI 代做成果 | High | Web Audio source／tests 可確認；Pure Data 操作影片與 poster 已存在；REAPER 仍只有使用者事實 | 對齊 9 階段 signal flow；加入可證明／分工／不能證明；PD 明載 AI assisted 與逆向重建，REAPER 明載尚未系統練習 | 已實作；PD 影片存在，原始工程證據待補 | `.pd`／`.rpp`／可歸屬錄音仍不存在；約 63 秒 PD 操作影片只證明本機功能測試，不能取代 Patch、獨立作者能力或使用者／學術驗證 |
| PA-035 | PUBLICATION_BOUNDARY | Public Git、submission alias、public assets | Public Repository 的 tracked hidden／internal／prompt／admission source 可讀；Vite 全量複製 `public/`；Hamlet limited-use rights gate 已通過 Phase B | 「未渲染／不進 bundle」可能被誤當保密，或把限定非營利核准擴張為 commercial clearance | Critical | GitHub visibility、tracked files、Vite 行為、manifest／workflow、applicant attestation 與 publication audit 已確認；private originals 未找到 | 保留 publication boundary audit、pre-upload gate 與限定用途；不自動改 visibility、history 或 Pages | Limited-use gate 完成；repository scope 持續治理 | `docs/admission/*` 已在 public Git；source-only 不是 private。本輪 `check:publication` exit 0，但新 PR 仍為 Draft、尚未 merge／deploy，且本機 gate 通過不等於公開核准 |
| PA-036 | EVIDENCE_GOVERNANCE | 《畫本》、MV remix 與未存在聲音 artifact | 當時 Repository／history 沒有可核對的作品或檔案 | 推論選件會製造作者角色、日期、權利或成果 | Critical | 此為歷史 inventory 結論；內容仍依賴使用者素材 | 當時採「不新增案例」並以 `EVIDENCE_REQUESTS.md` 列補件要求；較新的申請者事實已允許新增 text-only cases | 舊決策已被取代；文字型案例已存在 | 《畫本》→其他可驗證作品→個人 MV 的順序已採用；公開媒體、credit、日期與 rights 未核對前仍不得補寫成片證據或權利結論 |
| PA-037 | PERFORMANCE_REGRESSION | Admission sections 與 entry budget | 初版把研究構想與 AI／作者性直接併入 entry，draft entry 191880 B 超過 184320 B 預算 | 新敘事會讓現有 performance gate 失敗 | High | Fresh build 與 bundle audit 已確認 | 不提高預算；兩區各自 lazy-load，保留永久 anchor wrapper、deferred hash settle 與 section error boundary | 已完成 | 182055／161193 B 是中間 pass 歷史值；2026-07-25 current draft／submission entry 為 173631／152769 B，lazy 3D closure 維持 638680 raw／169383 gzip B |
| PA-038 | ADMISSION_NARRATIVE | 11 段高階 IA | `App.jsx`、Navbar 與 `llms.txt` 已接上 Hero → 問題意識 → 證據導覽 → Web Audio → Pure Data → 研究構想 → 代表作品 → 合作 → roadmap → AI → contact；`#selected-work` 內依序放《畫本》、既有詳細案例、指定 MV | 評審可先看最強聲音證據，再分辨學習、研究構想與跨域支持能力 | High | Source、scanner、fresh builds 與本輪四 viewport rendered behavior 已確認 | 保留 11 個穩定高階 ID、`#research-proposal` alias、lazy section wrapper 與 deferred hash settle | Source／自動驗證／本輪 Browser smoke 通過；輔具與實機矩陣待補 | 1280×800、768×900、390×844、320×720 均為 0 broken hashes／duplicate IDs／overflow；Pure Data、Hamlet、`#selected-work`、`#contact` fresh deep links 落在 fixed nav 下約 95–112 px |
| PA-039 | AUDIO_EVIDENCE | Pure Data v0.2.1 | 公開 MP4／poster 真實存在；資料記錄約 63 秒、四組模擬參數、Preset／Reset／Panic、輸出監看、AI-assisted 與逆向拆解界線 | 新增比純文字更強的操作證據，但容易被誤讀為獨立完成或正式驗證 | High | 檔案 inventory、source、build、public→dist hash、production HTTP與 Browser metadata已確認；Patch 作者能力、使用者／學術驗證未確認 | 標示 `學習中／可操作功能原型`、`尚待驗證`；明載 Patch 非獨立完成、目前輸入為模擬視覺參數、不是正式研究系統 | Media/build/hash／metadata 已驗證；實播與 fallback待補 | Browser 讀到 1276×720／62.983 秒、readyState 4、無 media error；poster、MP4 production 200。Save-Data、播放／鍵盤、失敗載入仍需核對；不得把介面 `validated` 當成驗證結論 |
| PA-040 | PRIVACY_MEDIA | Pure Data 原始錄影 | 畫面仍可見本機 D 槽專案路徑，且右側 Preset／下方 Patch 部分超出錄影邊界 | 可能暴露本機資訊，也降低作品集觀看完整度 | High | Source limitation 與影片內容由申請者提供；本輪公開接受決策未確認 | 最佳解是重錄完整框取且隱去路徑的 60–70 秒版本；未重錄前必須由申請者明確接受公開風險 | 未完成 | 不以 public copy 未重複路徑冒充影片畫面已清理 |
| PA-041 | CONTENT_GOVERNANCE | 代表作品、合作與學習路線 | 《畫本》、個人 MV、社團與工作經驗依申請者敘事加入；前兩件無公開成片，社團數字無 repository artifact | 可補足影音與協作脈絡，但精確角色、6→17 與權利主張需避免升格為獨立驗證 | High | Source 與使用者提供事實可確認；外部 artifact 未確認 | 《畫本》不主張得獎；個人 MV 只主張選曲／研究／取材／篩選／剪輯並揭露第三方權利；社團成長不歸因單一措施 | Source 已實作；證據待補 | 不嵌入未核准媒體；需補成片／credit／日期／rights ledger／社團紀錄 |
| PA-042 | RESEARCH_AI_BOUNDARY | `#research-positioning`、`#ai-workflow` | 研究構想分為問題、初步配置、可帶入能力、入學後補強；AI 區分協助、作者責任與尚需補強，包含 PD 失敗鏈；REAPER 過度敘述已降回實際學習狀態 | 降低把未建置系統、AI Patch、REAPER 安裝或可執行程式誤讀為研究成果／獨立熟練度的風險 | High | Source 與 scanner 已確認；研究可行性與作者熟練度未驗證 | 維持申請階段聲明與不能證明清單；不得宣稱完成多聲道、心理聲學、較低門檻成效、REAPER 作品或指導認可 | Source／本機驗證完成；內容簽核待完成 | 73/73 fixtures、67／9 rules 通過；對正向過度主張保持 fail closed，對明確否定 caveat 保持通過 |
| PA-043 | PUBLIC_METADATA_CONTACT | Metadata、`llms.txt`、`#contact` | title／OG／Twitter／JSON-LD 已同步申請者名稱與聲響／互動／數位學習；canonical、OG URL、Portfolio 與 public GitHub link 已填入；`llms.txt` 列 11 段；私人研究計畫 DOCX 實際存在但未公開 | 搜尋、分享與頁尾出口可與現行 IA 一致，不再依賴假 contact CTA，也不誤導私人檔案狀態 | High | Source、fresh build、scanner 與本機 Browser anchor 已確認；production HTTP／分享卡未重驗 | 只保留真實 Portfolio／GitHub；私人 DOCX 未形成核准公開審閱版前不提供下載，且不可寫成沒有檔案 | 本機驗證完成；production 待 Draft PR merge 後重驗 | Submission build 467 modules；`#contact` deep link 位於頂端約 95–112 px，0 broken hashes／duplicate IDs。Raster preview與跨平台分享仍待驗 |
| PA-044 | FINAL_VERIFICATION | 11 段 IA、submission artifact、publication gate、Browser matrix | 2026-07-26 `install`、`doctor`、scanner、draft／submission builds、public→dist inventory、publication gate 與四 viewport Browser smoke 已執行 | 該 fingerprint 的 build、artifact、limited-use rights 與 targeted rendered behavior 可確認；不得用來推論 2026-07-27 組合已通過，人工／實機、第三方 rights 與研究成效亦不得冒充通過 | Critical | 2026-07-26 命令輸出、artifact inventory、manifest 與 Browser runtime 已確認 | 保留自動／rendered／applicant／research 證據分層，明列 private-original、YouTube rights／credit 與人工測試缺口；整合後重跑 | 2026-07-26 基線完成；2026-07-27 待重驗 | `install`／`doctor` exit 0；18/18 sound、14/14 rights、73/73 scanner；draft 471／200889 gzip／180733 entry／44315 CSS，submission 467／192936／153704／44315；scan 132／25、67／9，public 118、0 missing／mismatch；publication exit 0。數字只適用 2026-07-26 fingerprint |
| PA-045 | GIT_DEPLOYMENT | PR #6／#7／#9、Actions、Pages、production | PR #6 與其 Pages run 的 deployment／HTTP 已確認；PR #7 Phase B 已進 2026-07-26 base，publication gate 位於 Configure Pages／upload 前；當時 PR #9 為 Draft 且 mergeable、本機兩個 gate通過但未 merge／deploy，PR #8 保持原狀 | 將歷史 production、base、本機驗證與 Draft PR 分層，避免把舊 exit 0 或 Draft PR 誤記為目前已上線／發布核准 | High | 2026-07-26 Git、PR、既有 Actions／Pages、workflow source 與本機 gate 可確認；2026-07-27 branch／PR 與 production deployment 未確認 | 重新查證目前 branch／PR，不直接 push `main`；核准 merge／deploy 後重驗 production | 2026-07-26 基線完成；目前 branch／PR／production 待重查 | 先前 HTTP 200 只屬舊 fingerprint；目前需經新組合 gates、核准 merge、deploy 與 production smoke 後才能稱為已發布 |
| PA-046 | PUBLICATION_BOUNDARY | Admission source／docs 與 public Git | `src/data/admission-*`、`src/data/ai-workflow.js`、`docs/admission/*` 與 Pd binaries 已在 public Git；submission alias／dynamic Draft split 只影響 bundle | Source-only 不能被誤當 private；limited-use approval 也不能擴張為完整著作權／商業權利 | Critical | public Repository、source boundary、manifest、attestation、workflow 與本輪 publication audit 已確認；private originals 未找到 | 保持 private workbench、`usedForExistingVideo: false`、`notValidated` 與限定用途；不要加入原始測試、個資或未核 rights 材料 | Limited-use gate 本輪通過；scope 持續治理 | `check:publication` exit 0、`verified / approved`；主要治理缺口是 private originals、Pure Data 路徑揭露、YouTube 第三方 rights／完整 credit 與 public source scope。Draft PR 不等於發布核准 |
| PA-047 | VISUAL_ACCESSIBILITY | 旗艦 Web Audio 視覺策略卡片 | 舊配圖帶有模板英文字與流程／架構誤標；底部 disclosure 只重複施工式圖像說明 | 評審可能把概念圖誤讀為已完成介面或技術證據，無功能感的箭頭也造成錯誤 affordance | High | N1／N2／N3 素材、目前 source、draft／submission build 與 targeted Browser 可確認 | N1／N2／N3 固定配對；輸出 561／1122 AVIF/WebP；使用正式文案與 alt；移除重複 disclosure、箭頭與互動提示 | 本機組合驗證通過 | Web Audio 案例在桌機與 390 px 行動版均為 section／page overflow 0；draft／submission build 通過。圖片內容與 alt 已核對，完整失敗載入／Save-Data 矩陣仍屬人工項目 |
| PA-048 | CONTENT_RESPONSIVE_ACCESSIBILITY | `data-visualization-cases` 製作流程與工具標籤 | 1200×900、內含文字的舊 SVG 被共用 4:5 `object-cover` 圖卡裁切，並以大畫布留下空白；同一案例又誤列 Power BI／Excel | 評審無法完整閱讀四階段，也會把另一件資料分析專案的工具誤認為本案例使用 | High | `productionWorkflow`、`CaseProcessSection`、draft／submission build 與目前 rendered behavior 可確認；完成投影片只作內容核對 | 移除舊 SVG；以 lazy HTML／CSS `section > ol > li > article` 呈現四階段；只從本案例移除 Power BI／Excel，保留另一案例標籤；不把完成投影片冒充原始流程證據 | 本機組合驗證通過 | 1440 px 為 4 欄，四卡各約 301×209 px；390 px 為單欄，四卡各約 330×199 px；兩者 section／page overflow 皆為 0。中寬 2 欄由 CSS 與 build 確認，仍需實機矩陣 |
| PA-049 | INFORMATION_ARCHITECTURE_VISUAL_ACCESSIBILITY | `#project-index` 代表作品／作品索引 | 舊卡片把大量施工資訊、未分級欄位與錯誤工具關聯帶進快速索引；兩張資料案例截圖分別含第三方介面與受限教育資料 | 評審難以快速比較四件代表作，錯誤工具或不適合公開的畫面亦可能造成可信度、隱私與權利風險 | High | Source、公開安全 derivatives、既有 rights evidence、submission artifact 與目前 Browser 可確認 | 固定 Web Audio → Hamlet → Power BI → 資料視覺化順序；使用短題名／摘要、精簡角色工具、真實 anchor 與靜態標籤；Web Audio 用 responsive AVIF/WebP，資料案例用自包含安全 SVG；`#project-index` 巢狀於 `#selected-work`，不增加高階 IA | 本機組合驗證通過；Hamlet 維持 limited-use `verified / approved` | 1440 px 四卡為 2×2、各約 875×611 px；390 px 單欄卡片寬 330 px；順序、四個有效案例入口、0 dead anchors、page／nav overflow 0 均確認 |
| PA-050 | AUTHOR_VOICE_CONTENT_GOVERNANCE | 全站公開文案、案例摘要與 ARIA／metadata | 同一事實可能在作者自述、可核對 artifact 與限制說明間失去層級，施工式字詞也會削弱第一人稱可信度 | 評審可能把作者選擇、工程可用性、研究驗證與權利核准混為同一種「完成」 | High | 公開 data modules、元件 copy、metadata、文件差異、submission text scan 與目前 Browser 已確認；最終主觀語氣簽核仍需本人完成 | 以作者行動 → artifact／方法 → 限制三層寫法統一；保留具名角色、`notValidated`、private-original 缺口、限定用途與團隊邊界；同步 visible copy、ARIA 與 metadata | Source、文件、本機自動與 targeted Browser 已完成；本人簽核待辦 | 逐句檢視 165 個含「我」的來源行，其中 75 個過量／模板式來源行已改寫；第三人稱代寫感為 10 個來源命中、合併為 7 組不重複字串。沒有新增參與者、成效、節省時間、商業權利、研究結論或官方認可 |
| PA-051 | FINAL_VERIFICATION_2026_07_27 | 作者視角、作品索引、案例流程與 submission artifact | 最新組合需要以同一 fingerprint 覆蓋文案、build、rights、scanner 與 responsive rendered behavior | 避免把 2026-07-26 基線、source inspection 或 Draft branch 誤寫成目前已驗證／已發布 | Critical | 目前工作樹的命令輸出、submission artifact 與 In-app Browser targeted smoke 可確認；production、輔具與實機不可確認 | 先完成衝突解析，再執行 `doctor`、publication gate、dist 定向文字檢查與 Git 一致性檢查；保留自動／Browser／人工／production 證據層級 | 本機自動與 targeted Browser 通過；production／人工矩陣待辦 | 完整 `doctor` exit 0。Draft：474 modules、entry 181772 B、CSS 46069 B、initial JS gzip 201686 B；submission：470 modules、entry 153688 B、CSS 46069 B、initial JS gzip 193282 B。Sound 18/18、rights 14/14、scanner 73/73；scan 135 files／28 text files、67／9 rules；`public/` evidence 為 3 direct copies、60 derivatives、16 WebVTT cues、63 Hamlet public files。獨立 `check:publication` exit 0／`verified / approved`。Browser：1440×1000 與 390×844 page／section overflow 0、4 個 index cards、4 個 workflow cards、0 dead anchors、console 0 warning／error；mobile menu label／展開／anchor landing 通過 |
| PA-052 | INFORMATION_ARCHITECTURE_RESPONSIVE | `learning-dashboard-analysis` | 舊版內容重複、正文偏左、右側空白與抽象圖權重過高，且缺乏完整案例研究順序 | 評審難以區分分析問題、方法、判讀、互動、倫理與反思，也可能把示意圖誤讀為成果 | High | 程式碼、submission artifact 與 5 viewport rendered behavior 已確認；受限真實媒體未進公開輸出 | 建立 lazy 專屬九章 renderer 與 case-scoped CSS；1240px／12 欄，低於 1024px 單欄，九章導覽在 1024+／640–1023／<640 為 3／3／1 欄；Hero、流程、概覽、互動使用正式分析敘事，公開限制集中第 08 節；不以抽象 SVG 或假播放器補成果證據 | 本機自動與 targeted Browser 通過；production／人工矩陣待辦 | Submission 1440／1280／1024／768／390：document／article overflow、out-of-bounds、text overflow 均為 0；九章為完整 3×3 或 9×1，3 問題、7 流程、3 圖表判讀、4 互動功能、3 倫理卡、4 後續步驟完整；console error／warning 0；冷啟動案例與 charts hash 均定位成功；`pnpm run doctor` exit 0，建議型證據缺件警告保留 |

## 仍需人工完成的驗收

- 2026-07-27 組合已在 1440×1000 與 390×844 完成作者視角改寫所影響之定向 smoke；2026-07-28 Power BI 專屬案例另以 1440／1280／1024／768／390px 完成 overflow、內容數量、console 與冷啟動 hash 檢查。兩者都不能取代 screen reader、真實 200% zoom、system reduced-motion、實機、多瀏覽器音訊、完整 media fallback／Save-Data 或 production field matrix。
- NVDA 或 VoiceOver 的 heading、landmark、live region 與 menu 閱讀順序。
- 瀏覽器真實 200% zoom、reduced-motion 系統偏好、iOS／Android 觸控與不同 Web Audio 實作。
- YouTube captions／transcript 的內容品質；Power BI 若要展示真實結果，仍需資料提供方另行明確許可。
- GitHub Pages 的 custom domain決策、正式 URL 上的裝置／輔具／field performance；canonical、workflow、Pages API與 project-site URL本身已確認。
- 旗艦聲響原型及 AI 介面的真實形成性使用者測試。
