# Codex 續作提示詞：南藝大聲響作品集

你正在延續同一個 GitHub Pages 作品集專案。不要重新開始，也不要重新設計已確認的架構。

依序執行：

1. 閱讀 `AGENTS.md`、`README*`、`docs/CODEX_HANDOFF.md`、`docs/PORTFOLIO_AUDIT.md`、`docs/CONTENT_MATRIX.md` 與最新 `git log`。
2. 檢查目前 branch、`git status`、未提交變更、既有 build/test 狀態。
3. 以 `docs/CODEX_HANDOFF.md` 中尚未完成的最高優先、目前不受素材阻擋的工作為本輪範圍。
4. 直接修改 repository；不得只產出計畫、audit 或範例程式碼。
5. 保留現有 stack、設計系統、使用者變更與 GitHub Pages 限制。
6. 不虛構作品、聲音成果、研究內容、技術熟練度或「自研 LLM」。
7. AI 協作只能作為方法論與開發紀錄，不能壓過作品本體。
8. 音訊不得 autoplay；Web Audio 必須由使用者手勢啟動，並提供 loading、error、media-unavailable 與靜態 fallback。
9. 可安裝或下載對呈現、測試或最佳化確有必要的可信開源套件／素材；記錄用途、來源、授權與影響，並避免不必要依賴。
10. 執行與變更相關的 formatter、lint、type-check、tests、build、browser／responsive 驗證。
11. 將本輪變更做成清楚 commits，具備權限時 push 既有 working branch 並更新 Draft PR；不得 merge、force push 或 production deploy。
12. 最後更新 `docs/CODEX_HANDOFF.md`，使下一個 Codex 工作階段能接續。

最後以繁體中文回報：

- 執行結果
- 本輪實際完成
- 檔案路徑
- 測試與建置結果
- UI／聲音／無障礙驗證
- 新增依賴與素材授權
- Git commit／push／Draft PR
- 尚缺素材與 blocker
- 下一輪起始指令

所有完成聲明必須有 repository、command、diff、commit、browser 或 PR 證據。現在直接開始執行。
